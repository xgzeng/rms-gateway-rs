[package]
name = "rms-gateway"
version = "0.1.0"
edition = "2024"

[dependencies]
tinc = "0.2"
prost = "0.14"
serde = "1.0"

axum = "0.8"
tokio = { version = "1.0", features = ["full"] }
anyhow = "1.0"

[build-dependencies]
tinc-build = "0.2"
